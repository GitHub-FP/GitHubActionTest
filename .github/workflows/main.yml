name: Greeting on variable day
on:
  workflow_dispatch
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - uses: actions/setup-node@v4 
        run: node -v
      - uses: actions/setup-node@v4
        run: npm -v
